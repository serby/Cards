# Override tree artifact outputs to avoid read-only permission errors when
# Xcode tries to install the app from DerivedData
common:rules_xcodeproj --define=apple.experimental.tree_artifact_outputs=0

# Make Bazel write output files as writable
common:rules_xcodeproj --experimental_writable_outputs
