# Task 02 — Validate Pilot

**Status:** done

## Command
```bash
make test-unit
```

Note: `test-unit` target doesn't exist yet — run the current `make test` instead until task 07 adds it.

## Pass criteria
- Both SettingsViewTests pass under Swift Testing
- No regressions in other (still-XCTest) files
- If this fails, fix before proceeding to task 03
