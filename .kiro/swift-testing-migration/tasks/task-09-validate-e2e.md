# Task 09 — Run make test-e2e

**Status:** pending

## Command
```bash
make test-e2e
```

## Pass criteria
- `CardsUITests` and `CardsUITestsLaunchTests` pass
- No regressions (UI tests unchanged — still XCTest)
